# Application Layer

Orchestrates the flow of data to and from the domain entities, implements use cases (such as scheduling alerts, querying status, commanding agents), and handles Command/Query separation (CQRS).
