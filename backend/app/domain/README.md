# Domain Layer

Contains Enterprise Domain entities, value objects, domain exceptions, and specifications representing the core logic of Endpoint Sentinel X. This layer has no dependencies on databases, UI, or frameworks.
